export default async ({__emit, __at, para, text, em, strong, val, m, heading, list, item, quote, codeblock, rule, comment, link, code, seq, ref, term, toc, glossary, notes, note, style, mathinline, mathblock, image, __region, __fence}, $) => {
__emit(__at(para(__at(text("窄栏网格换行、行号与行高亮："),0,42)),0,42));
__emit(__at(para(val((codeblock("json", "{\"description\": \"a deliberately long single line that must wrap on the column grid\", \"值\": \"中文按两列宽度参与网格计算，行会在合适的边界断开\", \"n\": 42}", {lineNo: 1, hl: "1"})))),44,274));
};
//# sourceURL=tsm:doc
