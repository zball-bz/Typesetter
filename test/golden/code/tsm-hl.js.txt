export default async ({__emit, __at, para, text, em, strong, val, m, heading, list, item, quote, codeblock, rule, comment, link, code, seq, ref, term, toc, glossary, notes, note, bibliography, style, mathinline, mathblock, image, __region, __fence}, $) => {
__emit(__at(para(__at(text("内置 tsm 文法：Typesetter 标记语言自举高亮。"),0,58)),0,58));
__emit(__at(val(await __fence("tsm", ({}), "= 标题与标签 <intro>\n\n正文有 *加粗*、`代码` 与 $a^2$；引用@intro，链接[站点](https://x.dev)。\n\n#!figure(src: \"a.png\", label: \"f\")\n图注 | 单元格\n#figure!\n\n```js\nconst x = 1;\n```\n\n- 列表项\n#let n = 42", 68)),60,302));
};
//# sourceURL=tsm:doc
