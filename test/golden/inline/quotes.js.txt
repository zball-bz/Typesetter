export default async ({__emit, __at, para, text, em, strong, val, m, heading, list, item, quote, codeblock, rule, comment, link, code, seq, ref, term, toc, glossary, notes, note, bibliography, style, mathinline, mathblock, image, __region, __fence}, $) => {
__emit(__at(para(__at(text("We treat these “spaces” as “homotopical” notions; don’t split at the apostrophe, and ‘single’ quotes behave the same."),0,131)),0,131));
__emit(__at(para(__at(text("中文里的“引号”仍然是全角标点，前后带可压缩的半角空隙；混排时“English inside”也按中文引号处理。"),133,273)),133,273));
};
//# sourceURL=tsm:doc
