export default async ({__emit, __at, para, text, em, strong, val, m, heading, list, item, quote, codeblock, rule, comment, link, code, seq}, $) => {
__emit(__at(para(__at(text("每个段落的首行都会缩进两个汉字的宽度，这是通过引擎内部的缩进块实现的，而不是样式表的文本缩进属性。"),0,147)),0,147));
__emit(__at(para(__at(text("第二个段落同样如此，缩进块参与断行计算，因此首行的可用宽度正确地减少了。"),149,257)),149,257));
};
//# sourceURL=tsm:doc
