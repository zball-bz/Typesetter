= 分页样例 <top>

分页是布局之上的后处理：内容按带切分，贪心装页，遇到孤行寡行、标题悬尾等违例时回退切点。这一段足够长，可以在窄页高下跨越页边界，从而测试寡行与孤行规则的实际效果，并让回退逻辑真正运行一次。

== 保持规则

标题不落页尾；下面的代码块按逻辑行切分：

```json
{
  "a": 1,
  "b": [2, 3],
  "c": "long enough to wrap at the narrow measure used by the tests"
}
```

#!figure(src: "photos/x.png", alt: "样图", w: 200, h: 80, label: "fig-p")
图与图注保持在同页。
#figure!

尾段：越过图之后继续排布，直至文档结束，引用@fig-p照常解析。
