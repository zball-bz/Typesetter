悬挂缩进与注释感知续行：

#codeblock("", [["    indented_call(alpha, beta, gamma, delta, epsilon, zeta_long_tail);"], ["value = 1; ", style({color: "var(--tsr-tok-comment)"}, "// a very long inline comment that wraps and its continuation aligns to the comment content column")]], {})
