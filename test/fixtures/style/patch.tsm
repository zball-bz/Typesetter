#{ $.style.push({ font: '"LXGW WenKai", serif', lang: "zh-TW" }); }

塊級作用域——這一段整體改用文楷與繁體語言標記，破折號與省略號……
照常走定義寬。

#{ $.style.popTo(0); }

回到默认样式的段落，行内有#style({color: "#a8432a"})[着色强调]、
#style({font: '"LXGW WenKai", serif'})[楷体片段]和#style({sizePx: 22})[放大片段]，
连同 *粗体* 一起参与断行。

#!aside(font: '"LXGW WenKai", serif', lang: "zh-TW")
區域級作用域：字體與語言一起換，內部照樣是普通標記。
#aside!
